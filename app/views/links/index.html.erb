<h1>Listing links</h1>

<table>
  <tr>
    <th>Url</th>
    <th>Name</th>
    <th>Saved</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @links.each do |link| %>
  <tr>
    <td>&nbsp<%= link.url %></td>
    <td>&nbsp<%= link.name %></td>
    <td>&nbsp<%= link.saved %></td>
    <td>&nbsp<%= link_to 'Show', link %></td>
    <td>&nbsp<%= link_to 'Edit', edit_link_path(link) %></td>
    <td>&nbsp<%= link_to 'Destroy', link, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Link', new_link_path %>
